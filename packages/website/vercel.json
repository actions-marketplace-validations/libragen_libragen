{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../.. && npm ci && npm run generate:schema && npx tsc --build && cd packages/website && bash scripts/build.sh",
  "outputDirectory": "dist",
  "framework": "astro",
  "installCommand": "echo 'Install handled in buildCommand'"
}
